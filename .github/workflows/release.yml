name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: Upstream version tag to build (for example, v2.7.2)
        required: true
        type: string

permissions:
  contents: write

jobs:
  build:
    env:
      RELEASE_TAG: ${{ inputs.version || github.ref_name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            label: windows
          - os: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            label: linux
          - os: macos-latest
            target: aarch64-apple-darwin
            label: macos-arm64
          - os: macos-13
            target: x86_64-apple-darwin
            label: macos-amd64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install dependencies (Linux)
        if: matrix.label == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Setup MSVC (Windows)
        if: matrix.label == 'windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Sync version from git tag
        shell: bash
        run: |
          VERSION="${RELEASE_TAG#v}"
          echo "Setting version to $VERSION"
          node -e "
            const fs = require('fs');
            for (const f of ['package.json', 'src-tauri/tauri.conf.json']) {
              const s = fs.readFileSync(f, 'utf8');
              fs.writeFileSync(f, s.replace(/\"version\": \"[^\"]*\"/, '\"version\": \"'+'$VERSION'+'\"'));
            }
          "

      - name: Install npm dependencies
        run: npm ci

      - name: Build Tauri app
        env:
          VITE_NSCB_BACKEND_REPO: ${{ github.repository_owner }}/nscb_rust
        run: npm run build -- --target ${{ matrix.target }}

      - name: Stage artifact (Windows)
        if: matrix.label == 'windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path staged | Out-Null
          Copy-Item "src-tauri/target/${{ matrix.target }}/release/nscb-desktop.exe" "staged/nscb-desktop-windows-amd64.exe"

      - name: Stage artifact (Linux)
        if: matrix.label == 'linux'
        run: |
          mkdir -p staged
          cp src-tauri/target/${{ matrix.target }}/release/bundle/appimage/*.AppImage staged/nscb-desktop-linux-amd64.AppImage
          cp src-tauri/target/${{ matrix.target }}/release/bundle/deb/*.deb staged/nscb-desktop-linux-amd64.deb

      - name: Stage artifact (macOS)
        if: startsWith(matrix.label, 'macos-')
        run: |
          mkdir -p staged
          cp src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg staged/nscb-desktop-${{ matrix.label }}.dmg

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: nscb-desktop-${{ matrix.label }}
          path: staged/
          retention-days: 1

  release:
    needs: build
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ inputs.version || github.ref_name }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts/

      - name: Flatten release artifacts
        run: |
          mkdir -p artifacts
          find release-artifacts -type f -exec cp {} artifacts/ \;
          ls -la artifacts

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag="$RELEASE_TAG"
          if gh release view "$tag" >/dev/null 2>&1; then
            gh release upload "$tag" artifacts/* --repo "${{ github.repository }}" --clobber
          else
            gh release create "$tag" artifacts/* --repo "${{ github.repository }}" --title "Release $tag" --generate-notes
          fi
